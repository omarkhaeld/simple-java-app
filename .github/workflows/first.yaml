name: firstworkflow
on:
 push:
  branches:
   - "main"
  jobs:
   firstworkflowjob:
    nmae: firstjob
    runs-on: ubuntu-latest
    steps: 
     -name: checkout steps
      uses: actions/checkout@v3
     -name: run maven
      run: mvn clean package
      shell: bash
